# Progress 2026-03-06 03:16:56

- Replaced the previous sharp anisotropy penalty in `shaders/renderer/gaussian_training_stage.slang` with a simpler L2 anisotropy loss on centered log-scales:
  - `log(scale)` is centered by its mean across XYZ,
  - the regularizer is `dot(centered, centered) / 3`,
  - autodiff integration in `csAdamStepFused` remains unchanged.
- Lowered the default scale regularization strengths to `1e-3` for both:
  - `TrainingHyperParams.scale_l2_weight`
  - `TrainingHyperParams.scale_aniso_weight`
- Updated matching defaults in:
  - `viewer.py`
  - `cli.py`
  - `README.md`
  - `doc/Training.md`
- Validation:
  - `python -m pytest -q`
