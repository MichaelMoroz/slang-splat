# Enable MVEE conic scanline binning

## Changes
- Re-enabled sampled5 MVEE conic scanline bin emission by passing `fitConic` to `emit_splat_bins` in `shaders/renderer/gaussian_pipeline.slang`.
- This now matches legacy behavior: both projection paths can use ellipse-aware scanline spans when conic validation passes.

## Validation
- `python -m pytest -q tests/test_renderer_pipeline.py` -> `5 passed`
- `python -m pytest -q` -> `11 passed`
