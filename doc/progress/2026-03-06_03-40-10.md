# Progress 2026-03-06 03:40:10

- Added dataset-driven COLMAP initialization estimation in `src/scene/colmap_loader.py`.
- New logic derives default init parameters from point-cloud nearest-neighbor spacing and requested gaussian count:
  - `base_scale`
  - `position_jitter_std`
  - `scale_jitter_ratio`
  - `initial_opacity`
- Added:
  - `suggest_colmap_init_hparams(...)`
  - `resolve_colmap_init_hparams(...)`
- `initialize_scene_from_colmap_points(...)` now resolves omitted init fields from those dataset-driven suggestions instead of fixed literals.
- Updated `cli.py` so omitted `--init-*` arguments remain unset and therefore use dataset-derived defaults.
- Updated `viewer.py` so COLMAP dataset load seeds the init sliders from dataset-derived defaults before the first training-scene initialization.
- Added tests covering:
  - count-dependent spacing suggestions,
  - manual override preservation in the resolver.
- Validation:
  - `python -m pytest tests/test_colmap_loader.py tests/test_training_kernels.py -q`
  - `python -m pytest -q`
