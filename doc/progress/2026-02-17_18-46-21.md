# 2026-02-17 18:46:21 - Remove max gaussian size parameter and clamp by screen size

## Summary
- Removed the explicit `max_splat_radius_px` parameter from renderer, viewer, CLI, shader prepass uniforms, and CPU reference projection.
- Projector radius capping is now derived from render target size (screen clamp), not a configurable maximum radius parameter.

## Implementation
- `src/renderer/gaussian_renderer.py`
  - Removed `max_splat_radius_px` constructor argument/state.
  - Removed `g_Prepass.maxSplatRadiusPx` uniform packing and cursor binding.
- `shaders/renderer/gaussian_types.slang`
  - Removed `maxSplatRadiusPx` from `PrepassParams`.
- `shaders/renderer/gaussian_project_stage.slang`
  - Added `screen_radius_cap()` from `g_Camera.viewport`.
  - Replaced radius clamp upper bound with screen radius cap for fallback and MVEE fit radius paths.
- `src/renderer/projection_sampled5_mvee_reference.py`
  - Removed `max_splat_radius_px` argument.
  - Radius upper clamp now uses `max(width, height)`.
- `src/renderer/reference_cpu.py`
  - Removed `max_splat_radius_px` from `project_splats(...)` API and forwarding call.
- `viewer.py`
  - Removed `Max Radius (px)` render UI control.
  - Removed renderer sync/create plumbing for this parameter.
- `cli.py`
  - Removed `--max-splat-radius-px` argument from both `train-colmap` and `render-ply`.
  - Removed constructor forwarding in both CLI workflows.
- Tests updated:
  - `tests/test_renderer_pipeline.py`
  - `tests/test_projection_sampled5_mvee_reference.py`

## Validation
- `.\\.venv\\Scripts\\python.exe -m pytest tests/test_projection_sampled5_mvee_reference.py -q` -> `4 passed`
- `.\\.venv\\Scripts\\python.exe -m pytest tests/test_renderer_pipeline.py -q` -> `9 passed`
- `.\\.venv\\Scripts\\python.exe -m pytest tests/test_training_cli_smoke.py -q` -> `1 passed`
