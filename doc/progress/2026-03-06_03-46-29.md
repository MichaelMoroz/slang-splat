# Progress 2026-03-06 03:46:29

- Fixed viewer-side dataset default propagation so COLMAP init defaults now actually react to the requested Gaussian count.
- `viewer.py` now:
  - tracks the last dataset-suggested init parameters,
  - refreshes auto/default init sliders when Gaussian count changes,
  - preserves manual slider overrides by only updating fields that still match the previous suggestion,
  - reapplies the latest dataset defaults right before training-scene initialization.
- Validation:
  - `python -m pytest -q`
