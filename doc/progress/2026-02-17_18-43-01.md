# 2026-02-17 18:43:01 - Remove anisotropy conic reject guard

## Changes
- Removed anisotropy reject condition from projection/binning conic validation in `shaders/renderer/gaussian_project_stage.slang`:
  - Deleted `minAxis >= MIN_AXIS_RATIO` requirement in `try_prepare_conic_for_binning(...)`.
- Removed now-unused shared constant `MIN_AXIS_RATIO` from `shaders/renderer/gaussian_types.slang`.

## Why
- Requested to fully remove the remaining anisotropy-limiting guard so highly anisotropic splats are not filtered out during conic preparation.

## Validation
- Ran renderer pipeline tests:
  - `.\\.venv\\Scripts\\python.exe -m pytest tests/test_renderer_pipeline.py -q`
  - Result: `9 passed`.
