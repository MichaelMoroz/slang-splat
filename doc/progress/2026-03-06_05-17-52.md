# 2026-03-06 05:17:52

- Replaced per-lane groupshared gradient atomics in the raster backward pass with wave-reduced atomics.
- `atomic_add_grad_shared(...)` now performs `WaveActiveSum(...)` per component and emits one groupshared `InterlockedAdd(...)` from the first active wave lane.
- Left the global gradient flush path unchanged; the optimization only targets groupshared contention in the backward raster accumulation step.
- Revalidated renderer and training kernel test slices after the shader change.
