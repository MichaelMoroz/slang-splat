# 2026-02-17 19:00:59 - Revert last 2 commits and fix persistent warming label

## Changes
- Reverted the last two commits as requested:
  - `b4530f8` reverted by `3791952`
  - `22a5e8c` reverted by `a5117f4`
- Fixed persistent UI `[warming]` state in `viewer.py` by enabling stats readback in both runtime render paths:
  - main view render now calls `render_to_texture(..., read_stats=True)`
  - debug view render now calls `render_to_texture(..., read_stats=True)`

## Why
- With `read_stats=False`, the renderer intentionally reports `stats_valid=False`, which keeps UI in `[warming]` mode continuously.
- Enabling stats readback restores expected behavior: a one-frame warmup, then stable stats.

## Validation
- `.\\.venv\\Scripts\\python.exe -m pytest tests/test_renderer_pipeline.py -q` -> `9 passed`
