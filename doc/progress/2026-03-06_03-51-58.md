# Progress 2026-03-06 03:51:58

- Corrected the dataset-driven COLMAP init jitter heuristic in `src/scene/colmap_loader.py`.
- `position_jitter_std` is now derived from an explicit occupancy model:
  - the target local cell width is the count-adjusted point spacing,
  - the Gaussian jitter std is set to the equivalent per-axis std of a uniform cube with that width,
  - `sigma = spacing / sqrt(12)`.
- This gives the jitter scale a direct spatial interpretation instead of the previous tiny ad-hoc factor.
- Validation:
  - `python -m pytest tests/test_colmap_loader.py tests/test_training_kernels.py -q`
