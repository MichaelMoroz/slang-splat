# Inline Shared Atomic Helper

## Summary
- Removed `atomic_add_grad_shared_scalar` and inlined its logic into `atomic_add_grad_shared<...>`.
- Preserved generic shared/global accumulation APIs and all gradient cache behavior.

## Changes
- `shaders/renderer/gaussian_raster_stage.slang`
  - Deleted `atomic_add_grad_shared_scalar(...)`.
  - Updated `atomic_add_grad_shared<let N : int>(...)` to perform zero-check + `InterlockedAdd` directly.

## Validation
- `tests/test_renderer_pipeline.py::test_raster_backward_smoke_and_determinism`
- `pytest -q` (full suite)
