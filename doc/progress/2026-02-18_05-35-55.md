# Progress 2026-02-18 05:35:55

## Implemented
- Added GPU scene initialization kernel in `shaders/renderer/gaussian_training_stage.slang`:
  - `csInitializeGaussiansFromPointCloud`
  - Inputs: preuploaded point positions/colors, init seed, init hyperparameters
  - Outputs: scene buffers (`position/scale/rotation/color_alpha`) + reset ADAM moments
- Added renderer scene binding helper in `src/renderer/gaussian_renderer.py`:
  - `bind_scene_count(splat_count)` allocates/sets active scene count without CPU scene upload.
- Extended `GaussianTrainer` in `src/training/gaussian_trainer.py`:
  - Supports construction without CPU scene uploads (`scene=None`, `scene_count=...`, `upload_initial_scene=False`)
  - Supports one-time pointcloud buffer binding/upload
  - Added `initialize_scene_from_pointcloud(...)` dispatch path for GPU-only reinitialization
  - Added kernel wiring for `csInitializeGaussiansFromPointCloud`
- Updated viewer COLMAP path in `viewer.py`:
  - Uploads COLMAP pointcloud buffers once on dataset load
  - Reinitialize action now uses trainer GPU initializer (no reinit-time point uploads)
  - Maintains scene count via lightweight proxy and syncs training renderer state to main renderer via GPU buffer copy

## Tests
- Added new test:
  - `tests/test_training_kernels.py::test_gpu_pointcloud_initializer_rebuilds_scene_without_cpu_upload`
- Regression runs:
  - `tests/test_training_kernels.py`
  - `tests/test_renderer_pipeline.py::test_raster_backward_smoke_and_determinism`
  - `tests/test_colmap_loader.py`
- Result:
  - All passed.
