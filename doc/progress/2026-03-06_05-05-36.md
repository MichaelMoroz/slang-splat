# 2026-03-06 05:05:36

- Removed duplicated Python raster layout constants from `GaussianRenderer`.
- Added a cached shader-source parser for `static const uint` raster constants in `shaders/renderer/gaussian_types.slang`.
- Switched renderer tile sizing and raster dispatch microtile sizing to use the parsed shader config.
- Added a regression test that checks renderer runtime raster config matches the shader-derived constants.
