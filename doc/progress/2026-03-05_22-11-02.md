# Progress Log - 2026-03-05 22:11:02

## Implemented
- Added `shaders/renderer/camera.slang` with:
  - `CameraParams`
  - `interface ICamera`
  - `struct PinholeCamera : ICamera`
  - methods for world/camera transforms, projection, unprojection, and ray generation.
- Refactored shader camera usage:
  - `shaders/renderer/gaussian_types.slang` now imports `camera.slang`, removed legacy free camera helpers, and uses `PinholeCamera` in `build_ellipsoid`.
  - `shaders/renderer/gaussian_project_stage.slang` uses `PinholeCamera` for camera-space and projection calls.
  - `shaders/renderer/gaussian_raster_stage.slang` uses `PinholeCamera.screen_to_world_ray(...)` for forward/backward ray setup.
- Reintroduced scale L2 regularization as post-ADAM decoupled decay:
  - `shaders/renderer/gaussian_training_stage.slang` adds `g_ScaleL2Weight`.
  - `csAdamStepFused` applies `scale *= max(1 - scale_lr * scale_l2_weight, 0)` after ADAM updates and before final clamps.
- Wired runtime/UI/CLI:
  - `src/training/gaussian_trainer.py`: `TrainingHyperParams.scale_l2_weight` + `g_ScaleL2Weight` binding.
  - `viewer.py`: added optimizer control `Scale L2`.
  - `cli.py`: added `--scale-l2`.
- Updated docs:
  - `doc/Rendering.md`
  - `doc/Training.md`
  - `README.md`

## Validation
- Ran existing tests (no new tests added):
  - `python -m pytest -q tests/test_renderer_pipeline.py` -> `9 passed`
  - `python -m pytest -q tests/test_training_kernels.py` -> `9 passed` (plus existing Pillow deprecation warnings)
  - `python -m pytest -q tests/test_training_cli_smoke.py` -> `1 passed`
