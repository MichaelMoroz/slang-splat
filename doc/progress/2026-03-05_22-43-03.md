# Progress Log - 2026-03-05 22:43:03

## Implemented
- Added autodiff anisotropy regularization in `csAdamStepFused` (no new kernels):
  - New uniform: `g_ScaleAnisoWeight`.
  - Added differentiable helpers:
    - `softplus_stable(...)`
    - `scale_aniso_loss(float3 scale)`
  - Uses smooth pairwise log-scale ratio penalty with threshold ratio `8`.
  - Injects autodiff scale gradient into `gradScale.xyz`.
  - Adds anisotropy scalar contribution to `g_LossBuffer` in the same kernel.
- Added training hyperparameter:
  - `TrainingHyperParams.scale_aniso_weight` (default `5.0`).
- Runtime wiring:
  - Bound `g_ScaleAnisoWeight` in trainer `_common_vars()`.
  - Updated `GaussianTrainer.step()` to read final loss after optimizer pass so loss/EMA include anisotropy term on finite-loss path.
- UI/CLI wiring:
  - Viewer optimizer control: `Scale Aniso`.
  - CLI flag: `--scale-aniso`.
- Documentation updates:
  - `doc/Training.md`
  - `README.md`

## Validation
- Executed existing suites (no new tests added):
  - `python -m pytest -q tests/test_renderer_pipeline.py`
  - `python -m pytest -q tests/test_training_kernels.py`
  - `python -m pytest -q tests/test_training_cli_smoke.py`
