# Progress Report - 2026-02-11 03:27:35

## Summary
- Removed the second writeback pass over `g_ScanlineWorkItems`.
- Added per-splat list-base storage and switched scanline items to store local offsets only.
- Kept single span solve per emitted scanline index.

## Changes
- `shaders/renderer/gaussian_types.slang`
  - Replaced `ScanlineWorkItem.baseIndex` with `ScanlineWorkItem.splatListOffset`.
  - Added `g_SplatListBases` buffer binding.
- `shaders/renderer/gaussian_binning_utils.slang`
  - Emits each `ScanlineWorkItem` once with precomputed local offset.
  - Reserves final list range once and stores base into `g_SplatListBases[splatId]`.
  - Removed second pass that reread and rewrote work items.
- `shaders/renderer/gaussian_project_stage.slang`
  - Compose computes final base as `g_SplatListBases[item.splatId] + item.splatListOffset`.
- `src/renderer/gaussian_renderer.py`
  - Added `splat_list_bases` work buffer allocation and shader bindings for project and compose stages.
- `doc/Rendering.md`
  - Updated stage descriptions to reflect per-splat base offsets + local scanline offsets.

## Validation
- `.\\.venv\\Scripts\\python.exe -m pytest -q` -> `13 passed`
