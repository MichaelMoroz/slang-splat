# Progress Report - 2026-02-10 07:30:29

## Summary
- Validated current scanline pipeline path on Vulkan with debug layers and stress rendering.
- Added explicit backend selection so Vulkan can be used from CLI tools and tests.
- Kept D3D12 as the default backend while enabling Vulkan opt-in.

## Code Changes
- `src/common.py`
  - Added `device_type_from_name(name: str) -> spy.DeviceType` mapping (`d3d12`, `vulkan`).
- `src/__init__.py`
  - Exported `device_type_from_name`.
- `render.py`
  - Added `--device {d3d12,vulkan}` and wired device creation through `device_type_from_name`.
- `viewer.py`
  - Added `--device {d3d12,vulkan}` and wired device creation through `device_type_from_name`.
- `tests/conftest.py`
  - Added backend selection via `SLANG_SPLAT_DEVICE` env var (defaults to `d3d12`).
- `README.md`
  - Updated runtime note and added Vulkan command examples for render/viewer/tests.

## Validation
- D3D12 test run:
  - `python -m pytest -q` -> `13 passed`
- Vulkan test run:
  - `$env:SLANG_SPLAT_DEVICE='vulkan'; python -m pytest -q` -> `13 passed`
- Vulkan runtime smoke and stress validation (manual script):
  - device creation, debug pipeline readback, render loop, and output readback succeeded.
