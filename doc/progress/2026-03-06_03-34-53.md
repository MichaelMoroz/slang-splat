# Progress 2026-03-06 03:34:53

- Updated `scale_aniso_loss` in `shaders/renderer/gaussian_training_stage.slang` to use a mean-normalized linear anisotropy penalty:
  - `meanScale = (sx + sy + sz) / 3`
  - `relativeScale = scale / meanScale - 1`
  - `loss = dot(relativeScale, relativeScale) / 3`
- This makes the anisotropy regularizer respond primarily to scale ratios instead of absolute scale magnitude.
- Updated `README.md` and `doc/Training.md` to describe the new ratio-based behavior.
- Validation:
  - `python -m pytest tests/test_training_kernels.py -q`
