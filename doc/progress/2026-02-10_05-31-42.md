# Slang Shader AGENTS.md Compliance Update - 2026-02-10

## Scope
Reviewed and adjusted Slang shader files for AGENTS.md compliance with no intended behavior changes.

## Changes

### `shaders/radix_sort/prefix_block.slang`
- Replaced hardcoded radix pack shift (`digit >> 2`) with `digit >> RADIX_PACK_LANE_BITS`.
- This removes a magic number and binds indexing logic to shared radix constants.

### `shaders/radix_sort/scatter.slang`
- Added `PACKED_QUARTER_COUNT` constant.
- Replaced hardcoded quarter-threshold logic (`< 1u`, `< 2u`, `< 3u`) with an unrolled constant-driven loop that produces the same packed increment mask.
- Updated quarter-offset shift to use `min(quarterIndex, PACKED_QUARTER_COUNT)` to keep index math explicitly bounded by named constants.

### `shaders/renderer/gaussian_mvee_utils.slang`
- Refactored second-moment accumulation from scalar-per-component (`s00/s01/s11`) to a vectorized `float3 secondMoment` representation.
- Refactored normalized conic reconstruction to `float3 conicNorm` and reused it directly for quadratic evaluation and output conic assignment.
- Kept the same fixed-iteration MVEE fitting pipeline and numerical safeguards.

## Regression
- Ran full regression suite: `pytest -q`
- Result: `13 passed in 6.14s`
