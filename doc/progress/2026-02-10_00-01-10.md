# Debug outline color + MVEE safety UI

## Changes
- Rasterizer debug ellipse outlines now use the corresponding splat color (`g_ScreenColorAlpha[splatId].xyz`) instead of a fixed debug color.
- Added UI slider in viewer render params:
  - `MVEE Safety`
  - range: `[1.0, 1.2]`
- Wired slider into renderer recreation and per-frame param application via `sampled5_safety_scale`.

## Validation
- `pytest -q` passed: `11 passed`.
