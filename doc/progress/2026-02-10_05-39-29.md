# Shader AGENTS compliance pass + regression run

## Summary
- Reviewed Slang shader files in `shaders/renderer` and `shaders/radix_sort` against `AGENTS.md` style constraints.
- Applied a targeted renderer cleanup to reduce scalarized per-component math in conic evaluation paths without changing behavior.

## Shader changes
### `shaders/renderer/gaussian_types.slang`
- Inlined conic gradient evaluation in `Ellipse.evalConic(float2 d)` with `float2x2` matrix multiply form to keep vectorized math without extra helper LOC.

### `shaders/renderer/gaussian_raster_stage.slang`
- Replaced duplicated scalar conic gradient math in ellipse debug rendering with the same `float2x2` matrix multiply form used in the ellipse helper path.

## Validation
- Regression suite executed with elevated permissions (required for Slangpy):
  - Command: `.\.venv\Scripts\python.exe -m pytest -q`
  - Result: `13 passed in 4.74s`
