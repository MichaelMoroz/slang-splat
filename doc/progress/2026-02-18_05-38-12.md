# Progress 2026-02-18 05:38:12

## Fix
- Patched `viewer.py` COLMAP load path to work with both reconstruction variants:
  - with cached `point_xyz_table` / `point_rgb_table`
  - without those attributes (fallback to `recon.points3d`).

## Details
- `_upload_colmap_pointcloud_buffers(...)` now uses `getattr(...)` and builds point arrays from `points3d` when cached tables are unavailable.
- Camera recentering on COLMAP load now also uses the same fallback path.

## Validation
- `python -m py_compile viewer.py`
- `python -m pytest -q tests/test_colmap_loader.py`
