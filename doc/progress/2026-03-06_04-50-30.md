# 2026-03-06 04:50:30

- Reworked raster execution to use `8x8` thread groups with fixed `3x3` per-thread microtiles for a `24x24` effective tile.
- Switched raster forward/backward dispatch to microtile thread counts and derived renderer tile sizing from the fixed shader layout.
- Changed raster batch staging to `64` gaussians so one thread stages one gaussian per batch.
- Rebuilt the backward raster loop to accumulate per-microtile gaussian gradients in registers before one shared accumulation per splat.
- Updated renderer pipeline tests for the derived tile API and added a partial-microtile CPU/GPU reference case.
- Refreshed `README.md` and `doc/Rendering.md` to describe the microtile rasterizer layout.
