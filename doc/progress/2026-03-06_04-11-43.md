# 2026-03-06 04:11:43 - Replace scale decay with log-reference regularization

- Replaced the old post-ADAM decoupled `scale_l2` shrink in `shaders/renderer/gaussian_training_stage.slang` with an autodiff log-scale regularizer around a scalar reference scale:
  - Added `g_ScaleRegReference`.
  - Added `scale_log_reference_loss(float3 scale, float referenceScale)`.
  - Regularization now contributes consistently to both scale gradients and reported total loss.
- Removed the old multiplicative scale-decay step from `csAdamStepFused`.
- Added trainer-side reference-scale tracking in `src/training/gaussian_trainer.py`:
  - Uses the initialization `base_scale` for GPU point-cloud initialization.
  - Uses an explicit constructor override when provided.
  - Falls back to the geometric mean of scene scales for prebuilt scenes.
- Updated `cli.py` so `train-colmap` resolves COLMAP init params up front and passes the resolved base scale into the trainer as the regularization reference.
- Added a regression test in `tests/test_training_kernels.py` verifying the scale regularizer pulls small and large scales toward the reference from opposite directions.
- Updated `README.md` and `doc/Training.md` to describe log-reference scale regularization and the hard anisotropy clamp.

- Validation:
  - `python -m pytest tests/test_training_kernels.py -q`
  - `python -m pytest tests/test_colmap_loader.py tests/test_training_kernels.py -q`
