# Progress 2026-02-18 05:16:40

## Implemented
- Switched training dataset textures in `src/training/gaussian_trainer.py` from `rgba32_float` to `rgba8_unorm`.
- Updated dataset upload path:
  - replaced float RGBA conversion with direct `RGBA` uint8 conversion.
  - ensured arrays are writable C-order copies (`np.array(..., copy=True, order="C")`) so `copy_from_numpy` accepts them.
- Applied to both native-resolution and train-resolution frame target textures.

## Why
- Reduces GPU memory usage for training images significantly versus float32 textures.

## Validation
- Ran:
  - `python -m pytest -q tests/test_training_kernels.py tests/test_renderer_pipeline.py::test_raster_backward_smoke_and_determinism`
- Result:
  - `9 passed`.
