# Debug View Letterbox Fit

## Changes
- Added a dedicated debug presentation path that preserves source aspect ratio while fitting to the full window.
- Implemented new compute kernel csComposeLetterboxDebug in shaders/renderer/gaussian_training_stage.slang.
- Updated iewer.py to route all debug views (Rendered, Target, Abs Diff) through the letterbox compose pass before final blit.
- Added a separate reusable full-window debug-present texture cache.
- Removed stale minInvScale field from debug abs-diff kernel uniform payload to match current shader struct.

## Result
- Debug image now fills the screen with locked aspect ratio and black bars where needed (no stretching distortion).
