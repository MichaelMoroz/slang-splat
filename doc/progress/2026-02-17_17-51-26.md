# Viewer/CLI Split + COLMAP Reinit Behavior

## What changed
- Removed mixed GUI/CLI behavior from iewer.py; it now launches GUI only (python viewer.py).
- Added new dedicated cli.py with subcommands:
  - 	rain-colmap: COLMAP training loop (former 	rain.py CLI path).
  - ender-ply: orbit-render snapshots for PLY scenes.
- Removed obsolete 	rain.py script.

## Viewer behavior updates
- load_colmap_dataset(...) now auto-initializes a training scene immediately after COLMAP load.
- Kept Init Training Scene button for manual regeneration.
- Added init-signature tracking to ensure changing init controls (including max gaussian count) triggers scene reinitialization on Start Training.
- This fixes the "max splat count change does nothing" workflow issue.

## Tests and docs
- Updated CLI smoke test to call cli.py train-colmap.
- Updated README and doc/Training.md command examples to cli.py.

## Validation
- Elevated CLI smoke: 	ests/test_training_cli_smoke.py passed.
- Elevated full suite: 25 passed.
