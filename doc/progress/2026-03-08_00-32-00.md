# 2026-03-08 00:32:00

- Replaced `cli.py`, `render.py`, and `viewer.py` with thin wrappers over shared application modules.
- Added `src/app` shared helpers for renderer/training parameter construction, snapshot writing, and scene-bound camera fitting.
- Split the viewer into `src/viewer/state.py`, `src/viewer/ui.py`, `src/viewer/session.py`, and `src/viewer/presenter.py`.
- Split COLMAP support into internal binary/type/ops modules behind the stable `src/scene/colmap_loader.py` facade.
- Simplified `src/renderer/reference_cpu.py` around a shared axis-parameterized scanline span path.
- Reduced `src/renderer/gaussian_renderer.py` and `src/training/gaussian_trainer.py` duplication by centralizing expression evaluation, buffer allocation tables, and repeated optimizer buffer bindings.
- Added `tools/complexity_budget.py` plus tests for the complexity scope and shared app helpers.
- Updated `README.md`, `doc/Rendering.md`, `doc/Training.md`, and `doc/SceneLoading.md` to describe the new structure and complexity-budget workflow.
