# Progress 2026-03-06 03:07:43

- Added `shaders/renderer/math_constants.slang` and moved shared shader math thresholds/constants there, renaming stage-specific names to domain-focused ones.
- Refactored raster shader traversal so the forward batch replay and reverse batch replay live in separate helper functions.
- Replaced `csRasterizeBackward` with `csRasterizeForwardBackward`, which recomputes the forward traversal inside the gradient pass and removes the per-pixel forward-state textures.
- Expanded training loss accounting to store total loss and raw image MSE separately, plus a GPU-reduced target signal max for host-side PSNR computation.
- Switched the image MSE loss gradient path and raster output-state gradient path to Slang autodiff where the code stays simple and the runtime cost is reasonable.
- Added trainer state tracking for `last_mse`, `ema_signal_max`, `last_psnr`, and `ema_psnr`, then surfaced smoothed PSNR in the CLI and viewer HUD.
- Updated renderer/trainer tests and documentation to match the fused raster gradient path and the new PSNR reporting.
- Validation:
  - `python -m pytest tests/test_renderer_pipeline.py tests/test_training_kernels.py -q`
  - `python -m pytest tests/test_training_cli_smoke.py -q`
