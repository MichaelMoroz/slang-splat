# 2026-02-17 18:30:12 - Gaussian count naming + anisotropy unclamp

## Changes implemented
- Renamed training init UI label from `Max Gaussians` to `Gaussian Count` in `viewer.py`.
- Renamed local init-flow variable usage in `viewer.py` from `max_gaussians` to `gaussian_count` to match semantics and avoid confusion.
- Removed scale anisotropy floor clamp from `build_ellipsoid` in `shaders/renderer/gaussian_types.slang`:
  - Deleted max-axis-ratio floor logic (`maxScale * 0.05`).
  - Ellipsoid scale now uses per-axis Gaussian scale directly (`gaussian.scale * radiusScale`) with only epsilon protection for inversion.
- Updated CPU projection reference for consistency in `src/renderer/projection_sampled5_mvee_reference.py` by removing the same `max_scale * 0.05` floor.

## Why
- Requested terminology update: control now represents direct initialization count, not an upper bound behavior.
- Rendering was enforcing a hidden anisotropy cap (20:1) through scale flooring, which can suppress intended elongated splats.

## Validation
- Ran focused tests:
  - `.\\.venv\\Scripts\\python.exe -m pytest tests/test_colmap_loader.py -q` -> `3 passed`
  - `.\\.venv\\Scripts\\python.exe -m pytest tests/test_renderer_pipeline.py -q` -> `9 passed`
