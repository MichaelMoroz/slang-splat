## Exposed Projection Mode In Realtime Viewer

Updated `viewer.py` so the realtime viewer can run either projection prepass mode.

Changes:

- Added `projection_mode` parameter to `SplatViewer` constructor.
- Passed `projection_mode` through initial `GaussianRenderer` creation.
- Preserved `projection_mode` in `_recreate_renderer()` so resize/recreation keeps the selected mode.
- Added CLI arg:
  - `--projection-mode {legacy_axis_extent,sampled5_mvee}`
  - default remains `legacy_axis_extent`
- Wired parsed CLI value into `SplatViewer(...)` construction.

Validation:

- Ran `python -m pytest -q`
- Result: `10 passed`
