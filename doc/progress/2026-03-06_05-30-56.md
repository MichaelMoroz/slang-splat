# 2026-03-06 05:30:56

- Switched COLMAP training target textures from `rgba8_unorm` to `rgba8_unorm_srgb`.
- Dataset images are still uploaded from RGBA8 bytes, but shader reads now use hardware sRGB decode.
- Added a regression test that asserts both native-resolution and training-resolution dataset textures use the sRGB format.
- Updated the README training note to reflect the sRGB target texture path.
