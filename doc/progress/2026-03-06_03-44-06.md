# Progress 2026-03-06 03:44:06

- Retuned the dataset-driven COLMAP initialization heuristic in `src/scene/colmap_loader.py` to reduce initial overlap.
- The new mapping now uses:
  - smaller base scales relative to point spacing,
  - lower position jitter,
  - lower scale jitter ratio,
  - slightly lower initial opacity.
- Added named constants for the heuristic, including an explicit assumption about the default renderer radius expansion so initial splat size targets are closer to non-overlapping after rendering.
- Validation:
  - `python -m pytest tests/test_colmap_loader.py tests/test_training_kernels.py -q`
