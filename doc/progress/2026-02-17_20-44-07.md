# 2026-02-17 20:44:07 - Increase Gaussian Count slider max to 2M

## Changes
- Updated `viewer.py` Gaussian initialization slider range:
  - `Gaussian Count` max changed from `200000` to `2000000`.

## Notes
- Internal initialization clamp remains `10_000_000`, so this only raises the UI upper bound.
