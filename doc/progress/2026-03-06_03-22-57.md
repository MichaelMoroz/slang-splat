# Progress 2026-03-06 03:22:57

- Renamed the shared `1.0 / 3.0` constant in `shaders/renderer/gaussian_training_stage.slang` from `RGB_CHANNEL_COUNT_INV` to `FLOAT3_COMPONENT_COUNT_INV`.
- Reason: the constant is now used both for RGB MSE averaging and for `float3` scale anisotropy averaging, so the old name had become misleading.
- Validation:
  - `python -m pytest tests/test_training_kernels.py -q`
