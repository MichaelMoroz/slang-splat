# Remove legacy ellipse projection path

- Removed legacy prepass implementation from `shaders/renderer/gaussian_project_stage.slang`.
  - Deleted `project_and_bin_legacy_impl`.
  - Kept a single prepass entrypoint (`csProjectAndBin`) backed by sampled5 MVEE projection.
  - Removed unused `csProjectAndBinSampled5MVEE` shader entrypoint.

- Simplified renderer API in `src/renderer/gaussian_renderer.py`.
  - Removed `projection_mode` constructor parameter and branching dispatch logic.
  - Renderer now always dispatches `csProjectAndBin`.

- Simplified viewer controls/CLI in `viewer.py`.
  - Removed projection mode combo box and projection-mode recreation path.
  - Removed `--projection-mode` CLI option and constructor plumbing.

- Replaced CPU projection reference in `src/renderer/reference_cpu.py`.
  - `project_splats(...)` now derives from `project_splats_sampled5_mvee(...)`.
  - Added conic reconstruction from MVEE ellipse axes/angle and applied radius inflation scale so CPU binning matches GPU sampled5 behavior.

- Updated docs/tests:
  - `doc/Rendering.md` now states sampled5 MVEE in Project+Bin stage description.
  - `tests/test_renderer_pipeline.py` no longer passes removed `projection_mode` argument.

- Validation:
  - `python -m pytest -q` -> `11 passed`.
