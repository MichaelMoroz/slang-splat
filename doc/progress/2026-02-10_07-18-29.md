# Progress Report - 2026-02-10 07:18:29

## Summary
- Reworked bin emission to append scanline work items in `emit_splat_bins` via `InterlockedAdd` on a scanline append counter.
- Added a dedicated scanline composition compute stage (`csComposeScanlineKeyValues`) that runs one thread per scanline item and writes final key/value pairs.
- Kept deterministic per-splat list reservation (`g_ListCounter`) so sorted-output behavior remains stable for parity tests.
- Switched default runtime backend to D3D12 to avoid Vulkan crashes.
- Installed test dependencies in the environment and validated the full suite.

## Shader/Renderer Changes
- `shaders/renderer/gaussian_types.slang`
  - Added `ScanlineWorkItem` struct.
  - Added `g_ScanlineWorkItems`, `g_ScanlineCounter`, and `g_maxScanlineEntries` uniforms.
- `shaders/renderer/gaussian_binning_utils.slang`
  - `emit_splat_bins(SplatBinRequest)` now emits scanline work items instead of writing final keys/values directly.
  - Preserved total-count reservation with `InterlockedAdd(g_ListCounter[0], totalCount, baseIndex)`.
- `shaders/renderer/gaussian_project_stage.slang`
  - Added `csComposeScanlineKeyValues` kernel and `compose_scanline_key_values_impl`.
- `src/renderer/gaussian_renderer.py`
  - Added scanline work-item buffer/counter allocation.
  - Added prepass counter reset for both list and scanline counters.
  - Added indirect dispatch setup from scanline counter and composed key/value dispatch before sort.
- `src/common.py`
  - Changed `create_default_device` default from Vulkan to D3D12.

## Documentation Changes
- `doc/Rendering.md`
  - Updated to a five-stage pipeline with explicit scanline composition stage.
- `README.md`
  - Added D3D12 runtime note and elevated PowerShell setup guidance.

## Validation
- `python -m pytest -q tests/test_renderer_pipeline.py` -> `7 passed`
- `python -m pytest -q` -> `13 passed`
