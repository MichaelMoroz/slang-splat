# Progress Report - 2026-02-11 03:54:08

## Summary
- Implemented tile-raster backpropagation with explicit reverse traversal of the batch loop.
- Added custom derivative load helpers for both global-buffer and groupshared raster loads.
- Added dedicated gradient-clear and raster-backward compute kernels plus Python orchestration and tests.

## Shader Changes
- `shaders/renderer/gaussian_types.slang`
  - Added backward input `g_OutputGrad`.
  - Added raster gradient buffers:
    - `g_GradSplatPosLocal`
    - `g_GradSplatInvScale`
    - `g_GradSplatQuat`
    - `g_GradScreenColorAlpha`
  - Added `groupshared uint gsBatchSplatId[RASTER_BATCH]` for shared-load gradient routing.
- `shaders/renderer/gaussian_math_utils.slang`
  - Marked `rotate_by_quat(...)` as `[Differentiable]`.
- `shaders/renderer/gaussian_raster_stage.slang`
  - Added `RasterState : IDifferentiable`.
  - Added differentiable primitives:
    - `raster_eval_alpha(...)`
    - `raster_blend_step(...)`
  - Added analytic undo helper `raster_undo_blend_step(...)`.
  - Added global and groupshared custom load wrappers with `[BackwardDerivative(...)]` and gradient accumulation.
  - Added `csClearRasterGrads` kernel.
  - Added `csRasterizeBackward` kernel that:
    - replays forward to reconstruct terminal state + processed prefix,
    - reverses batch traversal manually,
    - invokes `bwd_diff(...)` per reverse step.
  - Kept forward `csRasterize` behavior unchanged.

## Host Changes
- `src/renderer/gaussian_renderer.py`
  - Added kernel loading for:
    - `csClearRasterGrads`
    - `csRasterizeBackward`
  - Added gradient work buffer allocations.
  - Added `_clear_raster_grads(...)` and `_rasterize_backward(...)`.
  - Added `debug_raster_backward_grads(...)` helper to run backward pass and read gradients.

## Tests
- `tests/test_renderer_pipeline.py`
  - Added `test_raster_backward_smoke_and_determinism`:
    - executes backward raster pass,
    - asserts finite gradient outputs,
    - checks deterministic repeated run equality.

## Documentation
- `doc/Rendering.md`
  - Added "Raster Backward" stage description, dataflow, and gradient buffer contract.

## Validation
- `.\\.venv\\Scripts\\python.exe -m pytest -q` -> `14 passed`
