# 2026-03-06 05:23:24

- Removed the raster backward pass groupshared gradient cache and its cache-specific constants/storage.
- Changed per-splat backward accumulation to convert microtile-local gaussian differentials into global parameter gradients immediately.
- Kept wave reduction, but now the reduced values are written directly to the global gradient buffers instead of first landing in groupshared memory.
- Revalidated renderer and training kernel test slices after the cache removal.
